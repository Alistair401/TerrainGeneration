# TerrainGeneration
Creates a squre landscape using the diamond square algorithm.

The generated terrain mesh has UV seams for each quad allowing for optimised drawcalls and removes the need for massive terrain textures.

*Uses code from http://schemingdeveloper.com to recalculate normals.*
